name: courier
description: "DR-PHARMA Coursier — Application de livraison pour les coursiers."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  flutter_riverpod: ^3.1.0
  riverpod: ^3.1.0
  shared_preferences: ^2.5.4
  geolocator: ^14.0.2
  google_maps_flutter: ^2.10.0
  flutter_polyline_points: ^2.1.0
  url_launcher: ^6.2.2
  intl: ^0.20.2
  json_annotation: ^4.9.0
  freezed_annotation: ^3.1.0
  simple_barcode_scanner: ^0.4.0
  firebase_core: ^4.3.0
  go_router: ^14.2.0
  firebase_auth: ^6.1.0
  firebase_messaging: ^16.1.0
  infobip_mobilemessaging: ^9.6.1
  firebase_analytics: ^12.1.3
  firebase_crashlytics: ^5.0.0
  cloud_firestore: ^6.1.1
  firebase_storage: ^13.0.0 # Upload images/audio pour chat
  app_links: ^6.3.1
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.3.0
  workmanager: ^0.9.0+3
  flutter_local_notifications: ^18.0.1
  image_picker: ^1.1.2
  http: ^1.2.0 # HTTP client
  camera: ^0.11.0+2 # Pour la vérification de vivacité (liveness)
  package_info_plus: ^9.0.0
  share_plus: ^10.0.0
  path_provider: ^2.1.0
  path: ^1.9.0 # Path manipulation utilities
  battery_plus: ^6.0.0
  pdf: ^3.10.0 # Génération PDF pour export historique
  printing: ^5.13.0 # Impression et aperçu PDF
  csv: ^6.0.0 # Export CSV
  home_widget: ^0.7.0
  fl_chart: ^0.69.2 # Graphiques avancés pour statistiques
  flutter_tts: ^4.2.0 # Guidage vocal pour navigation
  map_launcher: ^3.5.0 # Lancer plusieurs apps GPS (Google Maps, Waze, etc.)
  connectivity_plus: ^6.0.0 # Monitoring de connectivité pour mode hors-ligne
  speech_to_text: ^7.0.0 # Reconnaissance vocale STT
  quick_actions: ^1.0.1 # Quick Actions iOS / App Shortcuts Android
  hive_flutter: ^1.1.0 # Stockage local pour mode hors-ligne
  audioplayers: ^6.1.0 # Sons personnalisés pour notifications + lecture audio chat
  record: ^6.0.0 # Enregistrement audio pour messages vocaux
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0

dev_dependencies:
  state_notifier: ^1.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  build_runner: ^2.10.4
  freezed: ^3.2.3
  json_serializable: ^6.11.2

  # App Icon Generation
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  web:
    generate: true
    image_path: "assets/images/logo.png"
    background_color: "#3D8C57"
    theme_color: "#54AB70"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
  macos:
    generate: true
    image_path: "assets/images/logo.png"
  image_path: "assets/images/logo.png"
  adaptive_icon_background: "#3D8C57"
  adaptive_icon_foreground: "assets/images/logo.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
