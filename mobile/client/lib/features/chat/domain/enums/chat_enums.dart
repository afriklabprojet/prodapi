/// Chat type enum — distinguishes delivery (temporary) from pharmacy (persistent) chats.
enum ChatType { delivery, pharmacy }

/// Lifecycle status of a chat session.
enum ChatSessionStatus { active, closed, archived }
